# Python Tutoring — правила работы

## Кто учится
Пользователь изучает Python ~2,5 месяца, проходит бесплатный курс на Stepik «Python для начинающих». По состоянию на 2026-08-09:
- Разделы 1, 2, 3, 4, 6, 7 — пройдены полностью.
- Раздел 5 «Итоговая работа на условный оператор» — пройден частично (25/45).
- Раздел 8 «Итоговая работа на циклы» — на Stepik засчитано частично (9/62: 8.1 — 9/12, 8.2 — 0/50), но по словам пользователя задачи 8.2 были решены — просто не защитались, потому что закончилось время экзамена (240 минут). Пересдавать не обязательно, это не пробел в знаниях.
- Раздел 9 «Строковый тип данных» — пройден полностью (методы строк, форматирование, таблица Unicode/ord-chr, срезы, ручной разбор и сравнение строк по составному ключу).
- Раздел 10 «Списки» — тема 10.1 «Создание и заполнение списков»: решено 22 задачи («Алфавит», «Список строк», «Список кубов», «Все сразу 1», «Список делителей», «Суммы двух», «Удалите нечётные индексы», «Символы всех строк», «k-ая буква слова», «Сумма квадратов», «Negatives, Zeros and Positives», «Без дубликатов» — на последней сперва пытался убрать дубли через `set()`, не сохранив порядок ввода; после разбора темы множеств переписал через список+`in` и решил верно; «Значение функции», «Remove outliers», «Google search - 1» решены чисто; «Google search - 2» — с трудом: перепутал формат ввода с задачей 1, `TypeError` при попытке сравнить список с строкой через `in`, перепутано направление `in` (`line in TRIGGER` вместо `запрос in line`), дважды забывал привести к одному регистру — решено пошагово с разбором `all()` на отвлечённом примере); 2026-07-30 добавлены «Построчный вывод» и «Windows OS» (чисто, с первой версии; `split()` и `split("\\")` + распаковка с `sep`) и «Инициалы» — застрял на генераторном выражении: `print` получил объект генератора вместо элементов, внутри — индексация списка строкой, ошибка не всплывала из-за ленивости; прямое «не понимаю что происходит», тема разобрана пошагово, после разбора решено через `range(len())` с индексом и лишним `0 + i`; «Диаграмма» — рабочее решение с первой версии (обход списка по элементам, `"+" * int(i)`), замечания только по именам (опечатка `nubmers`, `i` в роли элемента); «Корректный ip-адрес» — со второй версии: у проверки `len(lst_ip) == 4` не было ветки `else`, флаг оставался начальным `True` и «1.2.3», «1.2.3.4.5», «abc» давали «ДА» (тема флагов всплыла третий раз); 2026-08-01 «Добавь разделитель» — рабочее решение с первой версии, но список символов собирался вручную через `range(len())` + `append` и лишняя f-строка в `sep`; прямой вопрос «не понимаю, как тут использовать split или join» — `join` разобран с нуля (вызов у разделителя, приём строки напрямую), итог `separator.join(text)`; «Количество совпадающих пар» — верно с первой версии и без подсказок (вложенные циклы, внутренний правее внешнего), замечание только по читаемости: сдвиг индекса в теле цикла (`range(i, len(num) - 1)` + `num[j + 1]`) вместо границ диапазона; «Количество артиклей» — со второй версии: логика верная сразу (`lower().split()` + `.count()`), но вывод голым числом вместо требуемого текста и забытый отладочный `print`; «Все сразу 2» — со второй версии: методы (`extend`, `*= 2`, `insert`) верны сразу, но «удалить первый элемент» сделано как `del numbers[1]` (индекс переехал с прошлого шага) и стирало только что вписанную `17`; 2026-08-03 «Взлом Братства Стали» — тяжело, с четвёртой версии и с подсказками: старт с «даже не знаю с чего начать», затем `split("\n")` от одного `input()` (перевод строки `input()` не возвращает), `rfind("#")` вместо `find("#")` (решётки внутри самого комментария), `rstrip()` отсутствовал, а потом стоял только в ветке `if` — строки без комментария уходили с хвостовыми пробелами (мотив «не все ветки покрыты» из «Корректного ip-адреса», четвёртый раз); осталась неоптимальной связка `if/else` + `find` + срез вместо `split("#")[0].rstrip()`; 2026-08-04 «Переставить min и max» (🌶️) — со второй версии: логика обмена верна сразу (индексы взяты до перестановки), но `split()` не приводился к `int` и `min`/`max` сравнивали строки посимвольно; прямой вопрос «как привести список к int» — разобраны comprehension, `map` и `key=int`, выбран `map`; осталось лишним: временные переменные при `a, b = b, a` и `print(* nums)`; «Сортировка чисел» — со второй версии (вывод через `print(nums)` вместо распаковки), сам поднял вопрос об оптимизации по памяти, разобрано «метод на месте vs функция возвращает новый» (срез `[::-1]` тоже копия); «Сортируем песни» — со второй версии: вместо `.sort()` написана ручная сортировка выбором (`min` + `remove`) с лишним вторым списком, отсюда вопрос «как удалить список» (разобраны `del`/`clear`/`pop`); переписано на `[input() for _ in range(n)]` + `.sort()` — **списочное выражение применено самостоятельно впервые**; 2026-08-05 «Удалить первый символ» (условие требует списочное выражение) — чисто, с первой версии и без подсказок: `[element[1::] for element in keywords]`, обход по элементам, а не через `range(len())`; замечания косметические — лишнее второе двоеточие в срезе `[1::]` (третий раз) и перенос списка обратными слэшами с растущей лесенкой отступов; «Длины строк» и «Слова длиной не менее пяти символов» — обе чисто, с первой версии, без замечаний (`[len(element) for element in keywords]`; `[element for element in keywords if len(element) >= 5]` — первая фильтрующая часть comprehension, граница `>= 5` взята верно); «Числа-палиндромы от 100 до 1000» — со второй версии: до кода перепутал палиндром с простым числом, сама проверка `str(num) == str(num)[::-1]` написана верно сразу (первый comprehension по `range`, а не по готовому списку), но нижняя граница `range(1, 1001)` вместо `100` и `print(*palindromes)` вместо печати списка (зеркало «Сортировки чисел»); «В одну строку 1» — верно с первой версии, `print(*(input().split()), sep="\n")`, замечание только по лишним скобкам вокруг `input().split()`; 2026-08-06 «Списочное выражение 1» — со второй версии и с подсказками: первая печатала верно, но через `print(*[...], sep="\n")` вопреки условию (нужен цикл `for` и список в переменной), затем при правке пропали скобки `[]` и `print` получил `<generator object>` (повтор «Инициалов», прямое «не пойму почему ошибка»); после разбора и каркаса с пропусками — верно; «Списочное выражение 2» — верно с первой версии, без подсказок (`[pow(int(num), 3) for num in input().split()]` + цикл с `end=" "`), замечания стилевые (имя `nums` у списка кубов, `pow` вместо `**`, лишний финальный `print()`); задача-закрепление «Отчёт по медленным запросам» — тема «список vs генератор» закрыта (с первой версии, без подсказок; grade 3 из-за списка строк и `int()` внутри фильтра); задача-закрепление «Первая ошибка в логе выкатки» — тема «циклы и флаги» закрыта: `for...else` с `break` применён верно сразу, случай «ошибок нет» покрыт явно; grade 2 — со второй версии из-за ошибок вне темы (текст вывода с заглавной буквы и `"ERROR" in log` вместо `startswith`); 2026-08-08 начата сортировка пузырьком (тема курса) — coach-задача «Времена ответа — сортировка вручную» самостоятельно **не решена**, grade 1: первая версия без внешнего цикла (один проход, `3 2 1` → `[2, 1, 3]`, счётчик считал сравнения вместо проходов), вторая — верный каркас (внешний цикл, внутренний с границей `len - i - 1`, обмен распаковкой), но ранний выход поставлен на уровне пары (`else: break` внутри внутреннего цикла обрывал проход на первой упорядоченной паре) и флага обмена не было вовсе; после разбора трёх позиций флага и каркаса с пропусками — двукратное прямое «не понимаю, напиши код сам», выдан эталонный файл, итоговая версия собрана по нему; отдельно разобрана граница внутреннего цикла (`- 1` обязателен — пар на одну меньше, чем элементов; `- i` — оптимизация) и четвёртый раз подряд `print(список)` вместо `print(*список)` при требовании «через пробел»; 2026-08-09 Stepik «Сортировка выбором» (тема 10.2 «Алгоритмы сортировки») — самостоятельно не решена, потребовался эталон: первая версия сканировала список с нуля каждый проход и собирала максимум вместо минимума, вторая давала верный результат, но обменами (обмен внутри `if`, 2030 перестановок против 94), переменной-кандидата с индексом минимума не появилось ни разу — то же место, где 8 августа сломался пузырёк; разобраны цены версий в сравнениях и обменах и вывод «выбор экономит записи, а не сравнения, обе O(n²)»; в тот же день по своей инициативе дописал сортировку по длине слов к другой задаче — каркас выбора воспроизвёл по памяти верно, но сравнивал с `text[i]` вместо `text[min_index]`, исправил сам по одной подсказке; далее в тот же день шесть задач темы 10.1: «Список чётных» (с первой версии, но цикл с `append` вместо `list(range(2, n + 1, 2))`), «Сумма двух списков» (чисто, с первой версии; впервые после четырёх промахов верный `print(*...)`), «Сумма чисел» (чисто, с первой версии; `"+".join` к строкам и `int` только внутри `sum(map(...))`, замечание стилевое — смешаны f-строка и `sep=""`), «Самый длинный» (со второй версии: печатал слово вместо длины; списочное выражение, требуемое условием, так и не применено — оставлен `max(text, key=len)`, вариант через `[len(word) for word in text]` разобран отдельно), «Молодёжный жаргон» (работает с первой версии, но написано генераторное выражение вместо требуемого списочного — распаковка `*` скрыла разницу), «Валидный номер» 🌶️ (все 12 проверок проходит, но с двумя подсказками: генератор в `if` вместо `all()` и `len(number[2] == 4)` вместо `len(number[2]) == 4`; структура ветвления впервые выстроена самостоятельно и покрыла все пути — флаг `valid` стартует с `False`, ошибка «Корректного ip-адреса» не повторилась; после ревью сам переписал проверку длин на сравнение списков `[len(e) for e in number] == [3, 3, 4]`, сперва забыв, что во второй ветке групп четыре, и починил срезом `number[1:]`).

**Цель (уточнена 2026-07-24): оффер на стажировку Python-backend в крупной
компании** (Яндекс / Т-Банк / Авито / VK), целевое окно — весенний набор 2027;
далее — junior backend по итогам стажировки. Календарный план с вехами и
критериями готовности — [PLAN_TO_OFFER.md](PLAN_TO_OFFER.md).

### Профиль (собрано 2026-07-11, темп обновлён 2026-07-24)
- Опыт: Python — первый язык программирования, до этого не программировал(а) вообще.
- Стиль обучения: поровну теории и практики, без перекоса в одну сторону.
- Темп: 25+ часов в неделю, почти фултайм. Норма — 8–10 задач за сессию
  (до 2026-07-24 фактический темп был ~4 задачи в день при 10–12 ч/нед).
- Инструменты: с терминалом, Git и SQL пока не знаком(а) вообще — вводить с нуля, без каких-либо допущений о базовых знаниях.

## Структура папки
- `study_python/` — весь материал по изучению Python.
- `study_python/practice/` — отдельный файл на каждую задачу с курса (Stepik). Здесь пишем и проверяем код. Папки по разделам курса называются `NN_Название_раздела` (транслит, например `09_Strokovyy_tip_dannyh`), подпапки тем внутри раздела — `NN_MM_Название_темы` (например `09_05_Tablitsa_Unicode`); имена самих `.py`-файлов задач без числового префикса — тема уже видна из пути к папке.
- `study_python/practice/coach_tasks/` — задачи, которые генерирует коуч-скилл (не со Stepik), и код ученика по ним. Отдельно от файлов курса, чтобы не путать источники.
- `study_python/CODE_REVIEW_STANDARDS.md` — чек-лист критериев для ревью кода (PEP8, тайп-хинты, антипаттерны и т.д.), скопирован из стороннего open-source skill'а (MIT). Там же раздел «Принципы Карпатого» — простота, хирургические правки, проверяемая цель, без костылей.
- `study_python/KARPATHY_GUIDELINES.md` — полная копия гайдлайнов из [multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills) (MIT), референс; в работе используется адаптация из CODE_REVIEW_STANDARDS.md.
- `study_python/coach-skill/` — skill «python-practice-coach» (создан 2026-07-12): генерация практических задач, проверка решений (работоспособность → оптимальность → ревью), статистика, интервальное повторение, анализ трендов. **Когда пользователь просит задачу для практики, просит проверить решение, спрашивает про прогресс/повторение/тренды — прочитать `coach-skill/SKILL.md` и строго следовать ему.** Скрипты: `scripts/update_schedule.py` (SM-2, правит `progress.json`) и `scripts/build_site.py` (собирает сайт: генерирует константы в `roadmap_artifact.html` из JSON и вшивает код задач).
- `study_python/coach/` — данные тренера: `known_topics.md` (что изучено), `progress.json` (статистика и расписание повторений, правится только скриптом), `trends.md` (появится после первого анализа трендов), `activity.json` (журнал дней занятий для календаря на сайте: у каждого дня `level`/`tasks`/`note`/`items`, где `items` — список задач `{name, kind, file}`; см. «Отслеживание прогресса»), `practice_queue.json` (очередь задач на закрепление слабых тем — см. «Слабые темы и очередь закрепления»).
- `study_python/research-skill/` — skill «web-research» (создан 2026-07-15): веб-исследования через Firecrawl CLI с обязательным кэшем. **Когда пользователь просит найти/загуглить/исследовать актуальную информацию, собрать конспект по документации, узнать что актуально — прочитать `research-skill/SKILL.md` и следовать ему.** Для стабильных фактов (синтаксис, PEP8) в интернет не ходить.
- `study_python/research/` — кэш исследований: `INDEX.md` + файлы-отчёты. Проверять перед любым веб-поиском.

## Как проходит работа над задачей
1. Пользователь присылает скриншот задачи со Stepik (и, если есть, свой код).
2. Claude сразу, без напоминаний и без уточняющих вопросов, создаёт новый файл `.py` в `study_python/practice/` под эту конкретную задачу (если файла для неё ещё нет) — это стандартное поведение для каждой новой задачи. В начале файла — докстринг (`"""..."""`) с условием задачи (о чём задача, что на входе/выходе), чтобы пользователь мог свериться с условием прямо в файле, не пересматривая скриншот. Формат — как в `coach-skill/references/task-design.md` («Структура файла задачи»), без полей, которые не применимы к задачам со Stepik (`Тема`/`Сложность`/`Легенда` — только для coach-задач). Примеры входа/выхода — в кавычках, пояснения — `#`-комментарием рядом, чтобы их не приняли за требуемый вывод.
3. Если код пользователя работает — Claude проверяет и подсказывает, что можно оптимизировать / сделать более питонично, ориентируясь на критерии из [`CODE_REVIEW_STANDARDS.md`](CODE_REVIEW_STANDARDS.md) (PEP8, тайп-хинты, антипаттерны, сложность функций, докстринги — по мере того, как эти темы пройдены на курсе) и на раздел «Принципы Карпатого» там же (простота, хирургические правки, проверяемая цель, без костылей). Не переписывает всё с нуля без необходимости.
   **Вердикт — честный, и это правило шире coach-skill: на задачах со Stepik
   скилл не задействуется, а завысить оценку можно ровно так же.** «Чисто»,
   «без замечаний», «идеально» — только когда первая присланная версия
   заработала и оказалась оптимальной. Были неудачные попытки, содержательные
   подсказки или осталась неоптимальная конструкция — сказать об этом прямо:
   с какой попытки прошло, где понадобилась подсказка, что стоило написать
   иначе. Это не про строгость требований (планка остаётся мягкой — см.
   «мягко подталкивать» для тем, которые ученик видел, но не закрепил), а про
   точность формулировки: похвала, которой не соответствует реальность,
   лишает ученика ориентира.
4. Если пользователь не понимает задачу — Claude направляет вопросами и подсказками. **Решение за него не пишет.**
5. Исключение: как только пользователь полностью освоит Python, Claude переходит к написанию готового кода напрямую (по запросу пользователя), чтобы экономить время. До этого момента — только направление.
6. Наводящие вопросы (подсказки в стиле «а как думаешь, с чего начать» и т.п.) по умолчанию **не задавать** — пользователю они кажутся слишком очевидными/навязчивыми. Просто проверять код и по делу указывать на ошибки/что улучшить. Если для конкретной задачи наводящий вопрос будет уместен, можно предложить пользователю включить такой формат — но не включать самостоятельно.
7. Линтер/типчекер Python в VS Code (Pylance) отключён по просьбе пользователя (`python.analysis.typeCheckingMode: off`, `python.linting.enabled: false` в user settings.json) — волнистая подсветка мешала при обучении. Если Claude сочтёт, что подсветка ошибок снова пригодится, можно предложить включить обратно — но включать самостоятельно только если пользователь попросит.
8. **Эталонные файлы и примеры живут только до конца работы.** Файлы
   `<slug>_reference.py` и любые другие показательные примеры, которые Claude
   создаёт по ходу разбора, удаляются **при завершении работы над задачей или
   перед публикацией на GitHub** — сами, без напоминания и без вопроса. Ждать
   отдельного «я ознакомился» не нужно. Исключение одно: пользователь явно
   попросил файл оставить. Причина — в репозитории должен копиться код
   ученика, а не готовые решения рядом с ним: чужой код под рукой обесценивает
   собственную попытку при следующем заходе на ту же тему.
9. **Отступы — 4 пробела, докстринги без выравнивания.** В `.vscode/settings.json`
   репозитория зафиксировано `editor.detectIndentation: false`,
   `editor.tabSize: 4`, `editor.insertSpaces: true`. Без этого VS Code
   *угадывает* размер отступа по содержимому открытого файла и после `for ...:`
   вставляет случайное число пробелов. Настройку не убирать. Со стороны Claude
   правило то же, что и раньше (см. память): многострочные блоки внутри
   докстринга (Примеры, Вход, Выход, Правила) пишутся **flush-left**, без
   выравнивания продолжений под двоеточие — именно такие строки редактор
   принимал за образец отступа. Проверка на всякий случай: внутри докстринга
   не должно быть строк с ведущими пробелами, не кратными 4.
10. Инлайн-автодополнение кода (серые подсказки-призраки при печати, включая Copilot) отключено в user settings.json (`editor.inlineSuggest.enabled: false`, `github.copilot.enable.*: false`) — пользователь учится сам и не хочет, чтобы редактор дописывал код за него. Claude может сам предложить включить обратно, если сочтёт нужным, но включает только после согласия пользователя — не самостоятельно и не молча.

## Дорожная карта: Python backend developer
Собрано на основе распространённых roadmap'ов (roadmap.sh/python, roadmap.sh/backend и community-роадмапы 2026 года). Stepik-курс закрывает только базовый синтаксис (этапы 0-1) — дальше материалы будем добавлять сами по мере продвижения.

Сроки по этапам и критерии готовности каждой вехи — в [PLAN_TO_OFFER.md](PLAN_TO_OFFER.md).

0. **Синтаксис Python (сейчас, идёт по Stepik)** — типы, ветвления, циклы, строки, списки/кортежи, словари, функции, исключения, файлы.
1. **ООП и «продвинутый» Python** — классы, comprehensions, декораторы, генераторы, context manager'ы, itertools/functools. Дополняем Stepik, если курс не закроет тему глубоко.
2. **Git и контроль версий (стартует сейчас, параллельно с разделом 10)** — локально: коммиты, ветки, история. GitHub (аккаунт, push, публичный репозиторий) — не откладывать: на отборе смотрят историю коммитов, а она копится месяцами. Репозиторий, начатый в сентябре 2026, к марту 2027 выглядит как полгода регулярной работы; начатый в марте — как пустой.
3. **SQL и базы данных** — начать с SQLite, затем PostgreSQL: CRUD, проектирование схем, JOIN'ы.
4. **Тестирование** — pytest, привычка писать тесты для своего кода с ранних практик.
5. **Backend-фреймворк** — начать с FastAPI ⭐️ **(высокий спрос)** — типизация, асинхронность (async/await, asyncio), чище объясняет REST; тренды 2026 подтверждают порядок «SQL → FastAPI»: реальные джуниор-вакансии РФ просят FastAPI + базовый SQL. Затем при необходимости Django (чаще встречается в вакансиях РФ).
6. **REST API и аутентификация** — проектирование API, JWT/OAuth.
7. **Docker** — контейнеризация простого приложения.
8. **Кэширование и фоновые задачи** — Redis, Celery/RQ (ознакомительно).
9. **Деплой и CI/CD** — GitHub Actions, облако (ознакомительно, не глубоко).

**Алгоритмы и структуры данных (сквозная тема, ⭐️ критично для цели «стажировка»):**
сложность O(n)/O(n²), массивы и строки, два указателя, хеш-таблицы, стек и
очередь, бинарный поиск, сортировки, рекурсия, деревья и графы до BFS/DFS.
Идёт **параллельно** этапам 3-4, по 5-7 ч/неделю, стартует в январе 2027.
Причина отдельного этапа: отбор на стажировку идёт через алгоритмический
онлайн-тест **на время**, а не через портфолио, — и это главный фильтр.
Отдельная дисциплина внутри этапа — скорость под таймером (раздел 8 Stepik не
засчитался именно из-за нехватки времени на экзамене, при решённых задачах).
Формат тренировки и критерии готовности — в [PLAN_TO_OFFER.md](PLAN_TO_OFFER.md).

**Инструментарий (сквозная тема, ⭐️ высокий спрос 2026):** `uv` — менеджер пакетов и окружений (заменяет pip/virtualenv/pyenv; у ученика уже стоит — Python на ПК ставился через него), `Ruff` — линтер и форматтер (заменяет flake8/black). Консенсус 2026, часто в требованиях вакансий. Вводить по мере появления реальных проектов и окружений (≈этап 1-2). На горизонте — `Ty`, типчекер от Astral. Источник: [research/python-backend-trends_2026-07-15.md](research/python-backend-trends_2026-07-15.md).

**Даты этапов — в одном месте.** Календарь вех и критерии готовности —
[PLAN_TO_OFFER.md](PLAN_TO_OFFER.md) (источник правды). Машиночитаемая копия
для сайта — поле `deadline` у каждого этапа в `coach/activity.json` →
`progress.stages[]` (плюс `parallel: true` у этапов, идущих внахлёст с
соседними, и блок `progress.finish` — подача заявки и окно оффера). При
сдвиге сроков править **оба** места и прогонять `build_site.py`, не
замалчивать отставание. Отдельной «ближней цели» по разделу Stepik на сайте
больше нет — сайт показывает таймлайн этапов (решение ученика 2026-07-27).
Параллельный заход в ручное QA рассмотрен и отклонён 2026-07-26 в пользу
фокуса на backend (разбор — в `research/manual-qa-junior-market_2026-07-23.md`).

### Визуальная версия (Artifact)
Опубликована как интерактивная HTML-страница: https://claude.ai/code/artifact/77cd3cee-37f1-43c9-98a7-1c71a9fed8e1
Исходник — `study_python/roadmap_artifact.html` (в репо). Это приложение с
боковой панелью и четырьмя видами: **Обзор** (стрик со значками-молниями по
уровню, рекорд, счётчик задач, плитка «до ближайшего этапа», карточка «Общий
прогресс» = взвешенный % пути к стажировке + таймлайн всех этапов с датами
вех (у параллельных — пометка «параллельно», внизу строка финиша: подача
заявки и окно оффера), акцент-карточка «Далось труднее всего»
(авто-вывод самой сложной задачи из очереди), крупный мини-heatmap со сводкой
за 5 недель, короткая лента «Последняя активность» на 3 дня + ссылка в
Календарь), **Календарь** (heatmap дней занятий; будущие дни — пустые клетки,
текущий день — лавандовая обводка; клик по дню открывает детали со списком
задач — интенсивность подписана **словом** («обычная сессия», «плотная сессия»,
«большой день»), не «L2»; клик по задаче — модалку с её кодом (подсветка
синтаксиса + номера строк, один скролл) + ссылку на GitHub; ниже — полная лента
«Последние решённые задачи»), **Дорожная карта** (этапы 0–10, у каждой
карточки — дата вехи, конспекты тем в модалках) и **Закрепление** (сверху блок «Ждут решения» — приоритет; ниже —
закрытое, свёрнутое по темам-тегам `theme`; у каждой задачи сложность 0–10 с
прилагательным и «код ›»; плитка «ждут решения» прокручивает к блоку; третий
блок «Повторения по расписанию» — список уже решённых плановых SM-2-повторений
(из `ACTIVITY`, `kind: "повторено"`), отдельно от очереди `practice_queue.json`
— это другая система, за неё отвечает `progress.json`/`update_schedule.py`).

**Общий прогресс** считается из блока `progress` в `coach/activity.json`
(этапы roadmap с весами, статусами `done|current|todo` и `sub_pct` текущего):
общий % = Σ(вес завершённых) + вес_текущего × sub_pct/100. Обновлять статусы и
`sub_pct` по мере продвижения. Мини-полоса на «Дорожной карте» синхронизируется
с этим % автоматически. Там же у каждого этапа — `deadline` (дата вехи из
`PLAN_TO_OFFER.md`, `null` = вне плана) и необязательный `parallel: true`;
блок `progress.finish` задаёт строку финиша. Отдельного блока `deadline`
верхнего уровня больше нет — ближняя цель по разделу Stepik с сайта убрана.
**Только тёмная тема** — чёрный фон + фиолетовый акцент (светлая отключена
по просьбе ученика, `data-theme` из скелета игнорируется).

**Сборка — только через скрипт, не руками.** Константы `ACTIVITY`,
`PRACTICE_QUEUE`, `PROGRESS`, `TRACKING_SINCE` в HTML
**генерируются** `coach-skill/scripts/build_site.py` из `coach/activity.json` и
`coach/practice_queue.json` (между маркерами `/* @gen:start … @gen:end */`,
их не трогать). Скрипт заодно **вшивает код** каждой задачи в страницу (в т.ч.
код задач очереди закрепления по полю `file`) —
опубликованный артефакт на claude.ai не имеет доступа к локальным `.py`,
поэтому код должен лежать прямо в HTML. Порядок: правим JSON → запускаем
`build_site.py` (рабочим Python из [[python-interpreter-path]]) → редеплой
на тот же URL (не создавать новый артефакт; favicon 🐍 не менять). Мобильная
раскладка полагается на `<meta viewport>` из скелета артефакта — при
локальном превью через http.server его нет, это нормально.

## Пет-проект
Пока отложен — стартует не раньше этапов 5-6 (FastAPI, REST API). Когда пользователь дойдёт туда, Claude сам напомнит и поможет начать.

**Кандидат № 1 (записан 2026-07-15): чат-бот с долговременной памятью на [mem0](https://github.com/mem0ai/mem0)** — open-source библиотека «слой памяти» для AI-приложений (Python SDK `mem0ai`, 60k+ звёзд). Проект уровня junior-портфолио: HTTP-клиенты и работа с внешним LLM API, конфигурация и ключи, векторное хранилище, асинхронность, FastAPI-обёртка. Хорошо ложится на цель backend-разработчика: закрывает сразу этапы 5-6 roadmap и даёт актуальную AI-тематику. Регистрацию и API-ключи (mem0/LLM-провайдер) пользователь заводит сам, когда придёт время.

## Идеи для сайта (отложенные)

### Вкладка «Сортировки» — визуализация + трекер реализаций
Записано 2026-08-09 по просьбе ученика. Пятая вкладка в
`roadmap_artifact.html`: список алгоритмов сортировки, у каждого —
анимация на маленьком массиве, отметка «код написан / не написан» и
ссылка «код ›» на собственный файл ученика (как в модалках вкладки
«Закрепление»). Смысл — не картинка, а трекер: ученик пишет свою
реализацию каждого алгоритма, вкладка показывает, что уже закрыто.

Состав — все 11 алгоритмов, тремя группами (разбивка ученика):
- **Медленные:** пузырьком, выбором, простыми вставками.
- **Быстрые:** Шелла, быстрая, слиянием, пирамидальная, TimSort.
- **Не основанные на сравнениях:** подсчётом, блочная, поразрядная.

У каждой карточки — пометка глубины, они разные и путать их не надо
(оценка от 2026-08-09, под цель «алгоритмический тест на стажировку»):
- *писать руками* — пузырёк, выбор, вставки, быстрая, слиянием;
- *знать идею и сложность* — Шелла, пирамидальная, TimSort (плюс факт,
что за `sorted()`/`.sort()` стоит именно TimSort: гибрид вставок и
слияния, стабильный, O(n log n) в худшем случае);
- *знать, что существуют и когда применимы* — подсчётом, блочная,
поразрядная (обходят границу O(n log n), потому что не сравнивают
элементы, а раскладывают по корзинам; нужен ограниченный диапазон).

Референсы визуализаций (проверены 2026-08-09, все живые): основной —
[visualgo.net/en/sorting](https://visualgo.net/en/sorting) (свой массив,
пошаговый режим, подсветка текущей строки псевдокода; есть пузырёк,
выбор, вставки, слиянием, быстрая, подсчётом, поразрядная); дополняет
его [страница Галлеса](https://www.cs.usfca.edu/~galles/visualization/Algorithms.html)
(там Шелла, блочная, подсчётом). Проще всех —
[sortvisualizer.com](https://sortvisualizer.com/); с привязкой к коду —
[algorithm-visualizer.org](https://algorithm-visualizer.org/).

Данные — новый `coach/sorting_algorithms.json`, константа в HTML
генерируется `build_site.py` по образцу `PRACTICE_QUEUE`, код вшивается
по полю `file`. Тёмная тема и favicon 🐍 не меняются, редеплой на тот же
URL.

**Когда делать — две точки, Claude предлагает сам, не дожидаясь
напоминания:**
1. **Заготовка — сразу после того, как в разделе 10 закроется тема
   сортировок**, то есть когда будет написана сортировка простыми
   вставками (пузырёк и выбор уже есть: `coach_tasks/bubble_sort_latency.py`,
   `10_Spiski/10_02_Algoritmy_sortirovki/sortirovka_vyborom.py`). Момент
   выбран так, потому что все три медленные сортировки к этому моменту
   написаны своими руками и свежи в голове, а остальные восемь трогать
   рано. Живая анимация — только у трёх медленных, остальные карточки со
   статусом «январь 2027», без анимации.
2. **Наполнение — январь 2027, старт этапа 6 «Алгоритмы и структуры
   данных».** Быстрая и слиянием требуют функций и рекурсии — тем, до
   которых курс ещё не дошёл, поэтому раньше этой точки они в трекере
   были бы мёртвыми карточками.

## Отслеживание прогресса — обязательный шаг
Правило всегда активно (файл в контексте, не зависит от памяти между
сессиями). Триггер — **факт изменения прогресса**, не «конец сессии».

1. **Сразу правь «Кто учится» выше** (текущий раздел/тема, счётчик, дата «по
   состоянию на»), как только за сессию прогресс сдвинулся. Одна правка в
   открытом файле — не откладывать.
2. **Выгрузка — при обычном пуше** (отдельный пуш ради этого не делать;
   пользователь сам просит опубликовать в конце).
3. Сессия через `coach-skill` — прогресс уже пишет скрипт
   (`coach/progress.json`), не дублировать.
4. **Журнал активности (календарь на сайте) — писать СРАЗУ, не в конце
   сессии.** Как только за сессию написан код (не просто разговор) —
   немедленно добавить/обновить запись дня в `coach/activity.json`. Причина:
   пользователь пушит на GitHub несколько раз в день с двух устройств; если
   копить журнал «на конец сессии», дневные пуши покажут устаревший прогресс
   (был баг «0 задач сегодня»). Запись дня: `level` 1–4 (экспертная оценка:
   число задач + новизна/сложность темы), `tasks`, короткая `note` и массив
   `items` — конкретные задачи: `{name, kind, file}`, где `kind` ∈
   `изучено|пройдено|повторено|пример`, `file` — путь к `.py` от корня репо
   или `null`. Затем обновить сайт (см. «Визуальная версия»); при заметном
   изменении темпа пересчитать `progress` (`sub_pct`, статусы) в activity.json,
   а при сдвиге вех — даты в `PLAN_TO_OFFER.md` и `progress.stages[].deadline`
   (оба места). Шкала
   уровней: 1 — пара задач; 2 — обычная сессия
   (3–5 задач или новая тема); 3 — плотная сессия (6+ задач); 4 — большой
   день (10+ задач или несколько тем).

В начале следующей сессии — сразу опираться на обновлённые строки из раздела «Кто учится» и **самостоятельно** предложить продолжение (например: «в прошлый раз остановились на теме X, продолжим?»), не дожидаясь вопроса пользователя о том, где остановились.

## Слабые темы и очередь закрепления — обязательный шаг
Как и «Отслеживание прогресса», это правило всегда активно (файл загружен в
контекст) и не зависит от памяти между сессиями. Цель — Claude сам замечает,
где ученику тяжело, запоминает тему и **проактивно**, без просьбы, предлагает
задачу на закрепление. Очередь таких задач ведётся в
`coach/practice_queue.json` и показывается на сайте (вкладка «Закрепление»).

Разделение с `coach/progress.json`: там — оценки **решённых и отревьюенных**
coach-задач (SM-2, правится только скриптом). Очередь — это темы, где ученик
**застрял** и которые надо закрепить, пока задача не закрыта. За простое
затруднение оценку в progress.json НЕ писать.

**1. Детект затруднения (только явные сигналы).** Работает на **любой** задаче —
курсовой со Stepik, coach-задаче и на самой задаче-закреплении тоже (тема, на
которой ученик споткнулся повторно, тем более не закреплена). Отметить тему,
если за задачу было хотя бы одно из:
- ≥2 неудачных попыток / неверных решений на одной задаче;
- прямое «не понимаю», «не могу», «не получается»;
- понадобилось несколько содержательных подсказок, чтобы дойти до рабочего
  решения;
- тему пришлось разбирать пошагово.

НЕ считать затруднением: одну опечатку, поправленную сразу; один уточняющий
вопрос; аккуратное решение с первого-второго раза.

**2. Что делать при детекте.** Добавить/обновить запись в
`coach/practice_queue.json`. Дедуп по теме: если тема уже есть со статусом
`pending` — обновить её `reason` (не плодить дубли). Схема записи:
`id` (kebab-case), `theme` (короткий тег для группировки на сайте — «строки»,
«индексация и границы», «Unicode и коды», «циклы и флаги»…; решённые задачи
на вкладке «Закрепление» сворачиваются по нему; при отсутствии — «прочее»),
`topic`, `reason` (чем именно было тяжело), `difficulty`
(0–10, **относительная** — трудность темы для ученика на текущем уровне
знаний; рубрика и правило пересчёта — в `coach-skill/references/task-design.md`,
раздел «Сложность»; при обновлении `known_topics.md` пересчитывать difficulty
активных записей, чтобы освоенная тема не оставалась «сложной»), `status`
(`pending`/`in_progress`/`done`), `created` (ISO-дата), `file` (путь к `.py`
после создания задачи, иначе `null`). Затем синхронизировать вкладку на сайте
(см. ниже).

**3. Проактивное предложение (после каждой успешно решённой задачи).** Когда
ученик успешно закрыл задачу, проверить: есть ли в очереди `pending`-записи
(и, если удобно, просроченные повторения — `update_schedule.py due`). Если да —
предложить **ровно одну** задачу: краткая похвала + «помню, тема X давалась
тяжело — вот задача на закрепление, берёмся? да/нет».
- «Да» → сгенерировать задачу по Режиму 5 coach-skill, перевести запись в
  `in_progress`, записать `file`, обновить сайт.
- «Нет» → оставить `pending`; в этой сессии ту же задачу повторно не
  предлагать; при повторном отказе на другой сессии — деприоритизировать.

Не предлагать в момент, когда ученик сам застрял/фрустрирован — только после
успешного решения. Максимум одно предложение на решённую задачу; пачками не
заваливать.

**4. Закрывать тему только по факту закрепления.** Статус `done` ставится,
если задача-закрепление решена **без** детекта затруднения по п.1. Ученик
застрял на ней снова — запись остаётся `pending`, `reason` дополняется
деталями новой попытки, `file` сохраняется (ссылка на код прошлой попытки
остаётся живой). Закрыть тему, на которой только что споткнулись, — значит
потерять её: очередь существует ровно для того, чтобы этого не случалось.
Механика — Режим 5 coach-skill.

**5. Лимиты (чтобы не засорять сайт).** Держать не более ~3 активных
(`pending`+`in_progress`) тем и ~2 задач на тему. Закрытые (`done`) записи
оставлять до следующей сессии (для мотивации), затем подчищать, сохраняя
список компактным.

**6. Срок на закрепление (показывается на сайте).** У активных записей
(`pending`/`in_progress`) сайт сам считает дедлайн: `created` + запас по
`difficulty` — 0–2 → 3 дня, 3–5 → 7 дней, 6+ → 14 дней. В строке задачи
видно «в очереди N дн. · решить до <дата>» и пилюлю состояния (в срок /
осталось ≤2 дней / просрочено на N дн.), на «Обзоре» — плитка со счётчиком
активных тем и ближайшим сроком (краснеет, если есть просроченные).
Отдельного поля `due` в JSON нет — правило зашито в `roadmap_artifact.html`
(функции `pqAllowance`/`pqDue`), дата считается в браузере, поэтому статус не
устаревает между сборками. Решение ученика 2026-08-06: сроки нужны, чтобы
очередь не выглядела бессрочным списком.

**7. Синхронизация сайта.** После любой правки `practice_queue.json` — обновить
сайт (см. «Визуальная версия»).

Механика генерации самой задачи и запись оценки при закрытии — в coach-skill
(Режим 5 и Режим 2). Здесь — только детект, очередь и проактивное
предложение.

## Обновление правил
Это живой документ. Если в разговоре появляются новые правила или договорённости о процессе обучения — дописывать их сюда, а не переспрашивать заново каждый раз.
